# 开发用 patch：dsh web --patch 叠加到 web profile。
# 顶层必须是 loader patch 条目（PatchOptions）数组；insert 追加行。
# 插件 name 必须是 file:// URL（Windows 绝对路径不是合法 ESM specifier）。
- insert:
    - id: token-usage
      name: file:///D:/work/github/dsh/dsh-agent-toolkit/packages/token-usage/src/index.ts
      config:
        timezone: Asia/Shanghai
