# 发布历史

> 2026-09-10 自 AGENTS.md 拆出；每次发布后在此追加一条，保持最新在最前。

已发布（2026-09-14）：`dsh-agent-toolkit@0.4.0` 已发布到官方 registry（飞书运维指令面扩展：`/sessions` 列候选会话 + `/switch <序号|id前缀>` 切换绑定（live 接管复用、摘除 dispose 释放宿主写句柄、跨 chat 占用兜底）+ `/help`；`/doc <相对路径>` 项目内文件以飞书文件消息直发会话（共享护栏 resolveProjectPath：项目根内相对路径 + realpath 父目录防符号链接越界 + `feishu.docMaxBytes` 上限默认 30 MiB，应用需 im:resource 权限）；`/ls [相对路径] [关键字]` 目录罗列（树形连接符 + 类型图标 + 文件大小）与递归名称搜索（不分大小写、不追随符号链接、100 条/1 万遍历上限）；用户侧真实宿主验收通过；spec/plan 已归档 specs/archive/ 与 plans/archive/（2026-09-11 session-switch / doc-command / ls-command，2026-09-14 ls-tree-icons；message-queue 未实施未归档）。已发布（2026-09-10）：`dsh-agent-toolkit@0.3.0` + `@dsh-agent-toolkit/token-usage@0.5.0` 已发布到官方 registry（dsh 0.1.5-rc.1 兼容性升级，**依赖宿主 ≥ 0.1.5-rc.1，旧宿主不兼容**：persona Config `text`→`prefix`/`suffix`（飞书 `/new` "invalid config" 根因）；飞书卡片流式从已删除的 `assistant/chunk` 迁移到 `agent/assistant-stream`（start 帧校验 turn 建 attempt 基线，chunk 帧分流正文/过程区）+ 持久 `assistant/message` 结算对账「尾 text 段」有界恢复丢帧（`lastTextStep` 防护，宁缺勿错）；usage 启动回填迁 session-persistence 快照 API（`open/read/close`）；client-runtime 裁撤后的类型重定向；团队名册段 order 硬编码 116.6 改 `getSectionOrder('TOOL_SUBAGENT')+0.1` 动态跟随宿主段带；两包 vitest `css.include` 收窄规避宿主 `:where()` 选择器炸 jsdom；用户侧真实宿主验收通过；spec：docs/superpowers/specs/archive/2026-09-10-feishu-stream-0.1.5-design.md；升级台账：docs/superpowers/plans/archive/2026-09-10-dsh-0.1.5-compat-upgrade.md）。已发布（2026-09-08）：`dsh-agent-toolkit@0.2.9` 已发布到官方 registry（飞书卡片流式健壮性：拆卡定格旧卡状态行「📦 内容较长，已接续到下一张卡片」、卡片预算改真实 DSL 字节记账（`cardMaxBytes` 默认降 26000、新增 `cardPrintStep` 默认 5）、出站改确认式状态机（op 成功才提交状态；300301 视同成功 / 200850·200510 重激活重放 / 200860 废弃拆卡续写 / 未知错误 seq+2 重放 / commit 地板防 seq 回退）、reply 句柄准入后才替换且 `/new`/停 bot 等旧卡 finalize 后摘映射、retire 窗口重绑不复用已取消 runtime；含端到端字节级内容完整性守护测试；同版含飞书审批卡片；spec：docs/superpowers/specs/archive/2026-09-08-feishu-card-stream-robustness-design.md）。已发布（2026-09-04）：`dsh-agent-toolkit@0.2.8` 已发布到官方 registry（消息机器人列表行内删除（两段确认 UI，后端 DELETE 既有）；bot 自带 agentOptions 先移除后按人工验收恢复为**仅绑 main 可配**：绑 main 表单 Provider/模型必填、会话用 `bot.agentOptions ?? 宿主默认`，绑角色字段隐藏、编辑保存提交 `agentOptions: null` 清残留、会话用 `role.model ?? 宿主默认`；`/providers`+`/models` bots 端点随恢复回归；修复群消息 @ 校验误触发（比对本机器人 open_id）；spec：docs/superpowers/specs/archive/2026-09-04-bots-delete-and-remove-agentoptions-design.md（v2））。已发布（2026-09-03）：`dsh-agent-toolkit@0.2.7` 已发布到官方 registry（渠道发起人提示段：bot 会话 create/resume//new 三路径注入 `dsh-agent-toolkit:channel:sender` 段（order 20，"本会话由 {channel} 渠道的单聊会话发起。发起人 ID（{channel} open_id）：`{userId}`。"），供 Agent 写多维表格人员字段等场景取提问人 open_id；开关 `feishu.injectSender` 默认 true；设计 spec：docs/superpowers/specs/archive/2026-09-03-channel-sender-section-design.md）。同日早些：`dsh-agent-toolkit@0.2.6` 已发布到官方 registry（飞书入站支持 text/post/image 三类消息：图片渠道懒下载不占 WS 3 秒窗口 → 按魔数嗅探媒体类型 → `ctx.attachments`（**消息时惰性解析**，apply 期一次性捕获会拿到未注册的 undefined——差异家族第三例）→ image 内容块进 user message）。同日早些：`dsh-agent-toolkit@0.2.5`（团队名册段按 `tools.get(name, scope)` 联动工具可见性）；`dsh-agent-toolkit@0.2.4`（BASIC_TOOLS 迁至 tool-scope standing scope（祖先层）+ join 后 restrict，修复带工具白名单 bot 的 unknown global tools）；`dsh-agent-toolkit@0.2.3`（修复飞书 bot `/new` "处理失败"：BASIC_TOOLS 引用的 5+1 个宿主工具包补进 devDependencies + 守护测试；入站失败回复携带错误摘要；已读回执 no-op 消噪；**0.2.3 对带工具白名单的 bot 仍会失败**）。早前：`dsh-agent-toolkit@0.2.2`（2026-09-02，新增 `/create-agent` 交互式创建 Agent 命令；引导文本经 `agent.followup` 投递驱动主 Agent，落库复用面板 API 且 PUT 后经列表端点复核取证）；`@dsh-agent-toolkit/token-usage@0.4.1` + `dsh-agent-toolkit@0.2.1`（2026-09-01，修正 README 安装命令补 `--profile`）；token-usage 旧版 0.1.x/0.2.x 已 deprecate（`@dsh-agent-toolkit/prompt-stack`、`@dsh-agent-toolkit/project-bot` 从未发布，无需 deprecate）。
