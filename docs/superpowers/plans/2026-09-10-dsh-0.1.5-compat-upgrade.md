# dsh 0.1.5-rc.1 兼容性升级台账（进行中）

> 2026-09-10 起。触发：宿主升级到 0.1.5-rc.1 后飞书机器人 `/new` 报
> `invalid config: $.prefix missing required value`。本文件是交接台账：已完成/进行中/待办、
> 关键证据位置、续作指引。完成后并入 docs/domains/ 对应域文档并归档本文件。

## 环境变更（已完成）

- `deepseek-harness/` 已替换为 **0.1.5-rc.1 fresh clone**（tag `dsh-v0.1.5-rc.1` = commit `183f08e`，
  `.git` 恢复可用；此前 checkout 停在 0.1.0-rc.7 版本串 + 08-18 时间戳，`.git` 再次丢失）。
  旧 checkout 保留为 `deepseek-harness.old-0.1.2/`（未删，**验证全部通过后删除**）。
- 新 checkout 已 `pnpm install && pnpm run build` 成功；workspace 根 `pnpm install` 已重跑（link: 重绑）。
- **结构事实（0.1.5-rc.1）**：内置 agent presets 从 `apps/cli/config/agent-presets/` 迁至
  `packages/preset/agent-presets/presets/`；`packages/client/runtime`（dsh-client-runtime）**被裁撤**，
  类型拆到 `api/session-controller`、`api/workspace-controller`、`client/ui-session` 等；
  `Context.slots` 声明合并移到 `@deepseek-ai/dsh-client-ui-renderer/client`。

## 宿主 0.1.5-rc.1 破坏性变更清单（对照旧基线 0.1.2-alpha 时代，22 包全量 diff 结论）

1. **dsh-persona Config：`text` → `prefix`（必填）+ `suffix`**（packages/preset/persona/src/index.ts:50）。
   → 飞书 `/new` 失败的根因：toolkit 的 bot preset 组合行硬编码旧 `{text}`。
2. **session 事件删除 `assistant/chunk`**（只结算 `assistant/message` + 新增 `assistant/attempt`）
   → toolkit 飞书卡片流式打字机（channels/outbound.ts:85-108）失效。**未修，见待办④。**
3. **session-persistence 删 `readFrom`**（改 `open(id,'read')`→`SessionHandle.read`/`close`），
   `list()` 返回 `{header,...}[]` 快照。**已修（usage 回填）。**
4. **dsh-session 不再导出 `JsonValue`**（移到 `@deepseek-ai/dsh-util-values`）。**已修。**
5. SessionHeader：`version` 0→3、新增必填 `isSeeded`、删 `seedLength`；插件只读 id/cwd，仅测试夹具适配。
6. system-prompt Config：`persona` → `personaPrefix`/`personaSuffix`；段名 `deployment:persona` →
   `deployment:persona-prefix`（order 0）/`deployment:persona-suffix`（order 10200）；
   内置 subagent 指导段 order ~116.5 → 2800（**插件团队名册段硬编码 116.6 不再跟随，行为风险，未改**）。
7. ui-primitives：`MarkdownText` 的 `codeLabels` prop 改名 **`labels` 且必填**
   （`{code:{copyLabel,copiedLabel}, footnotes}`）。
8. 槽位 props 扩容：`GlobalStandardProps` 增 `useSessionPendingInteraction`/`usePanelInfo`；
   `SessionStandardProps` 增 `useConversation`/`useChat`；`ToolCallOwnerProps` 增必填 `loadImage`。
9. AgentPresets Config 增必填 `includeShippedRoot`；roots 前置 shipped root（shipped 优先同名 id）。
10. token-meter 新增 `static inject = ['sessionProjections']`（宿主官方 bundle 已含，暂无动作）。
11. subagent：`request.agentOptions` 需 provider 声明 `agentOptions` 能力（内置 spawn/fork 均已声明，无碍）。

确认兼容无需动作：storage-domain、credentials、llm、home-paths、attachment、agent-default-model、
commands、user-approval、webserver、ui-slots、agent-presets 服务面、agent-instructions、scope、
agent、tools、client-module 装载机制、SessionId 运行时值。

## 已完成修复（两包 typecheck 对 0.1.5-rc.1 全绿）

- ① `toolkit/src/channels/basic-tools.ts`：persona 行 `{text}` → `{prefix, suffix}`
  （对齐新 standard preset 拆法；prefix 含身份+直接提问指引，suffix 为 cwd 行）+ `bot-preset.test.ts` 同步。
  存量 `~/.dsh/.agent-presets/agent-bot/agent.cordis.yml` 每次启动重写，无需手工迁移。
- ③ `toolkit/src/delegate/tool.ts`：JsonValue 改从 `@deepseek-ai/dsh-util-values` 导入。
- ② `usage/src/usage/backfill.ts`：BackfillPersistence 结构面迁移（`list(options?)`→快照数组；
  `open(id,'read')`→`BackfillHandle{read,close}`；close 失败不回标 readFailed）+ `backfill.test.ts` 全量适配。
- 客户端类型重定向（client-runtime 裁撤）：
  `ISessions`/`SessionListState`→`@deepseek-ai/dsh-api-session-controller/client`；
  `WorkspaceListState`→`WorkspaceSnapshot`（`@deepseek-ai/dsh-api-workspace-controller/client`）；
  `SubagentAddress`→`@deepseek-ai/dsh-subagent/client`；
  `ToolCallBlock`→`@deepseek-ai/dsh-client-ui-conversation/client`；
  `SessionId`→`@deepseek-ai/dsh-session/types`（品牌类型，测试里 `'parent-1' as SessionId`）。
- 7 个 client index 加 `import type {} from '@deepseek-ai/dsh-client-ui-renderer/client'`（Context.slots 合并）。
- 两包 package.json：删 `dsh-client-runtime` link，新增 `dsh-api-session-controller` /
  `dsh-api-workspace-controller` / `dsh-util-values` / `dsh-client-ui-renderer` link；`dsh.client.inject` 同步。
- 测试夹具：`{persona:''}`→`{personaPrefix:''}`（6 文件）；AgentPresets 夹具补 `includeShippedRoot: false`；
  RUNTIME 桩补 `useSessionPendingInteraction`/`usePanelInfo`（5 文件）+ 委派卡桩补 `useConversation`/`useChat`/`loadImage`；
  assemble.test.ts 的 `deployment:persona` 断言改 `deployment:persona-prefix`。
- 委派卡 `MarkdownText` 补 `labels`（locale 新增 `card.copy`/`card.copied`/`card.footnotes`，zh/en 同步）。

## 进行中：usage 包 10 个客户端测试失败（jsdom 选择器）

- 现象：`entry.spec.tsx`(2) / `usage-entry.client.spec.tsx`(3) / `usage-modal.client.spec.tsx`(5)，
  统一 `SyntaxError: '*+:where(ul, ol))' is not a valid selector`（nwsapi，由 getByRole →
  getComputedStyle → jsdom 样式级联匹配触发；栈底是 `el.matches()`）。
- 已确认：新 ui-primitives `lib/markdown/MarkdownText.module.css` 含 `:where(ul, ol)`、
  `:has(+ :where(ul, ol))` 等**括号内带逗号**的选择器；jsdom 的 CSS 解析器（rrweb-cssom）朴素按逗号
  切分选择器列表 → 产生非法片段。旧版宿主 CSS 无此类选择器所以不炸。
- **未闭环**：`entry.spec.tsx` 只渲染 Tooltip+Button（Tooltip.module.css 干净），坏 CSS 的注入路径
  未确认。候选：(a) vitest `css.include` 处理链把某个被 import 的 CSS 全量注入；(b) usage 组件树
  （UsageModal/图表）间接引入 MarkdownText；(c) 其他被 import 的宿主 CSS。
  下一步：最小复现（单独渲染 `<Tooltip>` + getByRole）确认注入路径；修法候选：vitest css 处理
  改为跳过宿主包 CSS（`css.include` 收窄到本包 src），或在测试 setup 里 patch。
- 注意 usage vitest.config.ts 的 `css.modules.classNameStrategy: 'non-scoped'` 是 entry.spec 断言
  `rail` class 的前提，改动 css 配置时勿破坏。

## 待办（按序）

1. 修完 usage 10 个客户端测试（上述）。
2. `pnpm --filter dsh-agent-toolkit test` 全量（toolkit 449 个测试基线；client spec 可能同样踩 jsdom CSS 坑）。
3. **④ 飞书流式输出迁移（最大项，先出设计再动手）**：`assistant/chunk` 已删。候选新机制：
   订阅 `assistant/message`（内嵌 `stream`）/scoped `agent/assistant-stream` 事件，或轮询
   `session.seq`/`eventAt` 增量。需对照 0.1.5-rc.1 的 `packages/core/agent-loop/src/assistant-stream.ts`
   与 scoped-events 设计。落点：`toolkit/src/channels/outbound.ts` 的 chunk 消费链。写 spec 到
   `docs/superpowers/specs/2026-09-1x-feishu-stream-0.1.5-design.md`。
4. 风险跟进（可选）：团队名册段 order 116.6 与新宿主段落带（2800）错位 —— 仅提示词布局，不报错。
5. 全量验证：两包 test + typecheck + bundle（usage 先 bundle 再跑 toolkit 测试，AGENTS.md 有顺序约束）。
6. 删除 `deepseek-harness.old-0.1.2/`。
7. 文档同步：AGENTS.md（checkout 版本、preset 路径迁移、client-runtime 裁撤）、docs/domains/feishu.md
  （④完成后）、docs/usage/ 手册如有界面变化。
8. 发布：两包版本号 bump + `scripts/publish.ps1`（发布后追加 docs/domains/releases.md）。
9. 用户侧验证：装到真实 0.1.5-rc.1 宿主，飞书建 bot → 收发消息 → `/new`（AGENTS.md parity 清单）。

## 关键路径速查

- 0.1.5-rc.1 源码：`deepseek-harness/`（git tag dsh-v0.1.5-rc.1）
- 根因证据：`deepseek-harness/packages/preset/persona/src/index.ts:50`（`prefix: z.string().required()`）
- 飞书卡片流消费点：`packages/toolkit/src/channels/outbound.ts:85-108`（`assistant/chunk`）
- 新流式机制参考：`deepseek-harness/packages/core/agent-loop/src/assistant-stream.ts`
- usage 回填：`packages/usage/src/usage/backfill.ts`（已迁新 API）
- 台账本身：本文件；兼容性结论的域文档归宿：docs/domains/feishu.md + packages/usage/README.md
