# 团队模式组合：挂载 agent-team 插件。
# 注意：name 为指向入口文件的绝对路径——preset 行经 loader 按 preset 目录为锚 import，
# 绝对路径必须落到文件（Node/tsx 不支持目录 import，没有 package.json 查找）；
# 部署方 copy 本 preset 后需把路径改为自己机器上的入口文件位置。
- name: D:/work/github/dsh/dsh-agent-toolkit/packages/agent-team/src/index.ts
  # config 全省略 → teamsDir 默认 ./teams（相对本 preset 目录）
